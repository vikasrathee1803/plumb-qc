"""Migration Parity Validator (docs/PARITY-PLAN.md).

Proves a Tableau workbook's Snowflake sources show the same numbers on a
migrated (galaxy / presentation-layer) side as on the legacy side, via
read-only parity metrics snapshotted and compared through the existing
Plumb engine, baseline store, and report surfaces.
"""
